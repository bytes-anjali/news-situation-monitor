services:
  - type: web
    name: angelone-bytes-dashboard
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node build
    envVars:
      - key: OPENAI_API_KEY
        sync: false
